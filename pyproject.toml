[project]
name = "vela"
version = "1.0.0"
description = "Vela programming language compiler targeting a custom 16-bit ARM-like CPU"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
velac = "src.main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
